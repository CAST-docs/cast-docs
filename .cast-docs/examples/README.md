# Examples

Keep small, sanitized repository-specific examples here.

The public examples for CAST Docs live in the root `examples/` directory. Use this directory for examples that document repository-local conventions, not for generated output.

Generated local drafts belong in `.cast-docs/out/`.
