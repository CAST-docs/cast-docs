# Templates

Keep reusable repository-specific CAST Docs templates here.

This repository currently uses the built-in scenario fixtures under `examples/` as the canonical templates. Add files here only when the CAST Docs repository needs a durable local template that differs from the built-in examples.

Template files should stay valid or become valid after request-specific content is filled in, and they must use block types supported by `config/components.json`.
